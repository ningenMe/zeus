get:
  tags:
    - applicationMeta
  parameters:
    - name: applicationMetaId
      in: path
      required: true
      schema:
        type: string
  operationId: applicationMetaLatestGet
  responses:
    '200':
      description: ok
      content:
        application/json:
          schema:
            $ref: '../responses/ApplicationMetaLatestGetResponse.yaml'
