get:
  tags:
    - health
  parameters: [ ]
  operationId: healthGet
  responses:
    '200':
      description: ok
      content:
        application/json:
          schema:
            $ref: '../responses/HealthGetResponse.yaml'
